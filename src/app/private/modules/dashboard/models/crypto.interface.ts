export interface Crypto{
    crypto_id: string;
    crypto_name: string;
    crypto_value: number;
    crypto_stock: number;
}

export interface CryptoStock{
    crypto_id: string;
    crypto_stock: number;
}
